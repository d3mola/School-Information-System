/* eslint-disable */
// import deleteAStudent from '../controllers/studentController';
/*const deleteAStudent = require('../server');
$(document).ready(function() {
	$(".delete-btn").click(() => {
		$.ajax({
			type: 'GET',
			url: 'students/' + req.params.studentId,
			success: ((result) => {
				// $('#div1').remove();
				console.log(result)
			})
		});
	});
});*/